# Backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.4
itsdangerous>=2.1.2

# Voice feature — TTS (RVC voice-cloning + F5-TTS baseline run remotely on Colab,
# see colab/voice_server.ipynb)
edge-tts>=6.1.0
requests>=2.31.0

# RVC local fallback (voice/rvc_local.py) — used when the Colab endpoint above is
# unset/unreachable, so voice conversion/training still work on this machine's own
# GPU (if torch.cuda.is_available()) or CPU. TRAINING runs inside an isolated venv
# rvc_local.py builds on first use (git clone of RVC-Project/Retrieval-based-Voice-
# Conversion-WebUI + a handful of pip installs) -- needs `git` on PATH. No rvc-python
# package here: it depends on fairseq==0.12.2, which cannot even be imported on
# Python 3.12 (a hard language-level break in fairseq's own dataclass definitions,
# not a version pin problem); rvc_local.py's training uses the cloned repo's own
# train/*.py scripts instead, a fairseq-free pipeline.
# faiss-cpu IS a direct dependency here though (not left to the cloned repo's own
# requirements) because both the isolated training venv (--system-site-packages) and
# this process's own in-process CONVERSION (see below) need it for the FAISS
# retrieval index, and train/train_index.py needs it too.
faiss-cpu>=1.7.4

# CONVERSION (as opposed to training, above) runs in-process in this same app.py
# process/port instead of a separate venv+server — see voice/rvc_local.py's
# convert_local() docstring for the full story (a now-retired local_rvc_server.py
# used to run it from a separate process/port; that's no longer needed since the
# actual RVC-repo imports conversion needs — infer/audio.py, infer/vc/pipeline.py —
# don't touch the fastapi/pydantic/gradio pins that make training's own venv
# necessary). These three are what those imports pull in beyond what's already
# above (torch/faiss/soundfile/librosa/transformers): `av` and `ffmpeg-python`
# (audio decoding in infer/audio.py) and `praat-parselmouth` (imported
# unconditionally by infer/vc/pipeline.py even though the F0 method actually used,
# rmvpe, doesn't need it).
av>=11.0.0
ffmpeg-python>=0.2.0
praat-parselmouth>=0.4.3

# Not imported by any RVC code directly -- pinned here only because
# voice/rvc_local.py's _prepare_rvc_import() must import them itself, in this
# fixed order, before anything RVC-side (transitively, via librosa) gets a
# chance to import them in a different order: confirmed for real that the
# wrong ordering corrupts the process heap outright (Windows-only, "fatal
# exception: code 0xc0000374") partway through a native-extension import, no
# catchable Python exception, the whole interpreter just dies. See that
# function's docstring for the detail.
scikit-learn>=1.3.0
pandas>=2.0.0

# Manager dashboard "realism test" (engine/realism_engine.py) — speaker-embedding
# cosine similarity between a synthesized clip and the user's own uploaded samples.
# Pulls in torch; only imported lazily on first use, not at app.py startup.
# Decoding non-.wav original samples (.mp3/.webm/.ogg/.m4a) needs an `ffmpeg`
# binary on PATH (used by librosa, a resemblyzer dependency).
resemblyzer>=0.1.4
soundfile>=0.12.1

# Also imported directly (not just pulled in transitively via resemblyzer above) by
# voice/stt_adapter_infer.py and voice/stt_local_train.py to load audio at 16kHz mono
# for Whisper LoRA adapter inference/training — a separate direct use, both lazy.
librosa>=0.10.0

# Voice feature — STT (voice/stt.py, POST /api/transcribe), the input half of
# the voice loop. Also needs `ffmpeg` on PATH (Whisper shells out to it to
# decode webm/ogg/m4a/mp3). Model itself is loaded lazily on first use.
openai-whisper>=20231117

# STT Lab Tier 2 (voice/stt_local_train.py) — local LoRA fine-tuning of
# Whisper when a guest picks "local" (or "auto" falls back to it because
# Colab is unset/unreachable). Uses transformers' WhisperForConditionalGeneration,
# not openai-whisper above — PEFT only attaches to the HF model class. Both
# pull in torch already (see resemblyzer/openai-whisper), and are only
# imported lazily inside stt_local_train.train(), same pattern as the rest
# of this file's heavy deps.
transformers>=4.40.0
peft>=0.11.0

# Legal compliance — AI-generated voice disclosure + provenance tag prepended
# to RVC-cloned output (engine/voice_engine.py), required by Vietnam's Law on
# Artificial Intelligence (134/2025/QH15). Needs `ffmpeg` on PATH (already
# required above).
pydub>=0.25.1

# STT test tooling (tools/test_stt.py, tools/eval_stt_wer.py) — same installable
# SDK rag-legal-assistant uses, so these scripts exercise the real client path
# instead of hand-rolled requests calls.
-e ../clone-voice-client

# start_ngrok.py -- demo-only public tunnel, temporary stand-in until real deployment
ngrok>=1.7.0
